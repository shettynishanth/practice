#include<stdio.h>
void main()
{
    int arr[10],i,n,max,a;
     max=arr[0];
    printf("enter the element\n");
    scanf("%d",&n);
    for(i=1; i<n; i++)
    {
        scanf("%d",&arr[i]);
    }
    for(i=1;i<=arr[i];i++)
    {


    printf("%d",arr[i]);
    }

}
